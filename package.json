{
  "name": "repo-harness",
  "private": true,
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter template run dev",
    "test": "pnpm --filter repo-harness run test && make check"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
